# Prefer Mongo for channel metadata (same role Redis had — slug → peer id).
# Point at PeerPizza ChunkChat DB or any Mongo URI. TTL auto-expires channels.
MONGODB_URI=""
# CHANNEL_MONGODB_URI=""  # optional override

# Optional — only if you still want Redis instead of Mongo
REDIS_URL=""
PAGEVIEW_SALT=